import { Arguments } from 'yargs';
import { ProjectConfig } from '../../types/ProjectConfig';

/**
 * Handler for the `start` command.
 * @param args 
 */
const handler = (args: Arguments<ProjectConfig>): void => {
}

export default handler;
